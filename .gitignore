# Archived external research drafts (local only — never commit)
archive/

# Peer/research clones live in peer-research/ (local only — never commit).
# Do not put competitor clones in Vite's default public/ directory.
peer-research/
public/

# Dependencies
node_modules

# Build outputs
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Vercel
.vercel

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Secrets / local env (never commit)
.env
.env.*
!.env.example
*.pem
*.key
secrets.json

# OS / editor
.DS_Store
.cursor/
.vscode/


# Lint/scratch logs
lint-out.txt
lint2.txt
