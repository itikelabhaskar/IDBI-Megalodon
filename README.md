# IDBI MSME HealthLens

Bank-grade internal web app for IDBI credit officers — an explainable **MSME Financial Health Card** built from consented alternate data (GST, Account Aggregator bank, UPI, EPFO, **electricity / power consumption**, fuel / operational-spend signals, bureau-lite). Built for IDBI Innovate 2026, Track 03 (Financial Inclusion / Digital Lending / Credit Decisioning).

> HealthLens is a decision-support and configurable recommendation engine. IDBI remains the regulated entity and final decision owner. All data shown is synthetic prototype data.

**Live demo:** [https://idbi-megalodon.vercel.app](https://idbi-megalodon.vercel.app)

## Honesty

- **Synthetic book only** — HealthScores, BRE outcomes, fraud flags, and portfolio KPIs are computed on generated MSMEs, not IDBI production data.
- **Connector stubs** — AA / GSTN / UPI / EPFO / DISCOM / fuel / FASTag·e-way mocks / bureau are labelled Synthetic in-app (Governance + Architecture). No live ULI/OCEN claim.
- **Model metrics** — AUC/KS in `docs/model-card.md` are held-out results on synthetic labels. They are illustrative for the prototype, not a production performance guarantee.
- **Incomplete** — thin consent sets surface as a first-class decision, not a soft Approve.
- See `docs/SETUP_GAPS.md` for the pilot gap list and `docs/DEMO_SCRIPT.md` for a fixed judge walkthrough.

## Stack

- TanStack Start (React 19) + Vite
- TypeScript, Tailwind CSS v4, shadcn/ui, lucide-react
- Recharts for visualizations
- Zod for schema validation

## Getting started

```bash
npm install
npm run dev      # local dev server
npm run build    # production build
npm run lint     # lint
npm test         # Vitest
```

## Project layout

- `src/routes/` — pages (TanStack file-based routing); the nine credit-officer screens
- `src/components/healthlens/` — domain components (queue, health card, decision, explainability, fraud, CAM, governance)
- `src/components/ui/` — shadcn primitives
- `src/lib/types.ts` — the frozen `MsmeCase` data contract (engine output shape)
- `src/lib/data/` — deterministic synthetic data engine (archetypes, generator, dataset; leakage-free)
- `src/lib/scoring/` — features, rule-based scorecard, transparent ML viability model, decisioning / BRE
- `src/lib/connectors/` — Account Aggregator / GSTN / UPI / EPFO / DISCOM power / fuel / FASTag·e-way mocks / bureau connector stubs
- `src/lib/api/` — ULI / OCEN-style scoring endpoints (Zod-validated)
- `docs/` — Track 03 problem statement, model card, setup gaps, demo script

## Documentation

- `docs/problems.md` — IDBI Innovate 2026 Track 03 problem statement and expected outcome
- `docs/model-card.md` — ML model card (generated by `npm run train`)
- `docs/SETUP_GAPS.md` — what is stubbed vs needed for sandbox
- `docs/DEMO_SCRIPT.md` — scripted path on the live workbench

## Deployment

`npm run build` produces a Vite + Nitro build. The default target is **Vercel** (emits the Vercel
Build Output API to `.vercel/output/`); set `NITRO_PRESET` to target other hosts.

### Vercel (default, verified green)

The build targets Vercel out of the box and Nitro emits `.vercel/output/`. To deploy:

1. Push to GitHub.
2. On [vercel.com](https://vercel.com) → **Add New → Project** → import this repo.
3. Framework Preset **Other**, Build Command `npm run build`, Install Command `npm install`.
4. Deploy — Vercel auto-detects `.vercel/output` and serves it. (`vercel.json` already pins
   `NITRO_PRESET=vercel`.)

Dependencies are bundled into the serverless function (`noExternals`), which sidesteps the
Nitro-3.0-beta `@vercel/nft` tracing bug — so the serverless build is reliable.

### Cloudflare / AWS / Node (alternative targets)

The server is a portable Nitro handler. Set `NITRO_PRESET` before `npm run build`:

- `cloudflare-module` → `npx wrangler deploy` (Cloudflare Workers)
- `node-server` → run the Node bundle in `.output/` (Docker / ECS / any VM)
- `aws-lambda` → AWS Lambda + CloudFront / Amplify

All prototype data is synthetic and in-process, so no managed datastore is required.

### Cloud portability

HealthLens is cloud-agnostic: the frontend compiles to static client assets and the server is a
portable Nitro handler with presets for Vercel, AWS Lambda, Node, Azure, Netlify, and Cloudflare —
the same codebase deploys to any of them with only a preset change.

## Status

Track 03 MVP — full engine complete (data → features → scorecard → ML → decisioning → API → UI → governance), with an authenticated credit-officer workbench, maker–checker workflow, Incomplete evidence gates, authenticity bands, inclusion lift, and live applicant scoring.
